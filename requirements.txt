# Direct dependencies, human-readable. VERSIONS LIVE IN requirements.lock.txt,
# which is what the image installs -- see the note at the top of that file.
# Regenerate the lock after changing anything here:
#     .venv/bin/python -m pip install -r requirements.txt
#     .venv/bin/python -m pip freeze > requirements.lock.txt
google-adk
# Memory Bank and Agent Engine live here; google-adk does NOT pull it in,
# and without it memory writes fail at runtime rather than at import.
google-cloud-aiplatform
google-genai
google-cloud-firestore
# Imported directly for ClientOptions (regional endpoints, quota project).
#
# PINNED BELOW 2.35. That release breaks every Firestore call with
#   InvalidArgument: 400 Invalid database id %28default%29
# regardless of how the database is passed -- "(default)", None, and omitting
# the argument all fail identically, so it is not a usage error. Isolated on
# 2026-08-25 by toggling this package alone three times against a live
# Firestore read; 2.34.0 reads fine, 2.35.0 fails, 2.34.0 reads fine again.
# firestore 2.29.0 is unaffected once this is correct.
google-api-core<2.35
opentelemetry-sdk
opentelemetry-exporter-gcp-trace
pydantic>=2
pytest
pyyaml
google-cloud-modelarmor
# Chirp voices for parent notices.
google-cloud-texttospeech
# Coordinator dashboard (Cloud Run service; the tick job does not use these)
fastapi
uvicorn[standard]
# Imported directly for BaseHTTPMiddleware (security headers).
starlette

# Persisted parent-facing audio. Chirp runs in the tick container, whose disk
# does not survive the job; without this the recorded path outlives the bytes.
google-cloud-storage
