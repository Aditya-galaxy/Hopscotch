# Narration audio — Chirp3-HD

Generated by `scripts/narrate.py` using the project's own `media.speak()`, which
is the same Chirp3-HD synthesiser that reads statutory notices to families. The
demo is narrated by the voice the product ships.

**Total 3m 51s across ten sections**, measured with `afinfo`, against a 4:00 cap.
That leaves nine seconds of headroom, which is the point: the screen should be
allowed to breathe between sections rather than racing the audio.

| File | Runs | Cue it against |
|---|---|---|
| 01-problem | 21.6s | the landing page, letting the 60-tick clock fill |
| 02-what-this-is | 27.9s | scroll to the two halves |
| 03-run-the-fleet | 12.0s | pressing "Run a tick now" — start it here, come back at 09 |
| 04-the-clock | 30.4s | walkthrough steps 1–3 |
| 05-the-fleet | 26.5s | steps 4–5, the agent table |
| 06-the-human | 25.1s | steps 6–7, the letter and the family page |
| 07-the-money | 31.8s | steps 8–9, the claim checks |
| 08-refused | 18.9s | step 10, the blocked document |
| 09-google-cloud | 19.8s | Cloud Run executions, Scheduler, Logs Explorer |
| 10-close | 17.0s | back to the landing page |

`full-narration.mp3` is all ten joined, for a straight read.

**Playing it during an unedited take.** Play a section, let it finish, click to
the next screen, play the next. The judging criteria ask for a live unedited
demo — a voiceover is fine, splicing the screen recording is not. Section files
exist so you can pace the screen to the audio without ever cutting the video.

Regenerate with `.venv/bin/python scripts/narrate.py`. It caches by content
hash, so unchanged sections cost nothing.
