# Devpost image gallery — captions

1200x800 (3:2), captured from the live deployment with headless Chrome, not
mocked. Upload in this order; Devpost shows the first as the card image if no
video is set.

| File | Caption |
|---|---|
| 01-problem | Sixty days from a parent's signature. Miss it and the district owes compensatory services and faces a due process complaint. |
| 02-demo-start | One consent form, followed all the way through — ten screens, no login, real records. |
| 03-clock-starts | The clock runs from the day the district *received* consent, not the day the parent signed. That is 34 CFR §300.301(c)(1)(i). |
| 04-the-fleet | Five agents, and the gateway hands each a different shape of the same record. Casework gets 9 fields; the family-facing agent gets 4 and no consent block at all. |
| 05-the-letter | The fleet drafts and queues. It never decides to contact a family — a named person releases every letter. |
| 06-family-receives | The parent's own page: one child, theirs. Another family's case returns 404, not 403, because 403 would confirm that child exists. |
| 07-claim-checks | The same session as a Medicaid claim line. Eight rule checks, and a ninth a model performs: does the note describe the service the IEP authorised? |
| 08-refused | A consent form with instructions buried in it, refused by Model Armor before any extractor saw it. |
| 09-dashboard | What a coordinator sees. The identity switcher is at the top — the same records change shape for each role. |
| 10-parent-view | The family surface. A status page, and it says plainly that it is not the record: FERPA gives parents the right to the whole file. |
