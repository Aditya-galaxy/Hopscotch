# Copy to .env and fill. Never commit .env.
GOOGLE_CLOUD_PROJECT=
# Model location. Gemini 3.x and Gemma are served ONLY from `global`;
# a regional call 404s even though models.list() shows them there.
GOOGLE_CLOUD_LOCATION=global
# Model Armor is genuinely regional and has no global endpoint.
MODEL_ARMOR_LOCATION=us-central1
GOOGLE_GENAI_USE_VERTEXAI=true

FIRESTORE_DATABASE=(default)
CASES_COLLECTION=cases
AUDIT_COLLECTION=audit
DEADLETTER_COLLECTION=deadletter

# Model Armor template. Created by deploy/day1.sh; defaults to
# agentx-skill-review when unset.
MODEL_ARMOR_TEMPLATE=agentx-skill-review

# Vertex AI Agent Engine instance hosting Memory Bank.
# Create with: ./deploy/create_agent_engine.sh
AGENT_ENGINE_ID=

# --- authentication -------------------------------------------------------
# Both are REQUIRED when REQUIRE_AUTH=true. verify() refuses rather than
# skipping a check when either is missing: an unset OAUTH_CLIENT_ID would mean
# the token's audience is never validated, and an unset ALLOWED_DOMAINS used to
# mean every verified Google account was admitted.
OAUTH_CLIENT_ID=
ALLOWED_DOMAINS=district.example.org
# Least privilege. Anyone not named in ROLE_ASSIGNMENTS gets this.
DEFAULT_ROLE=liaison
ROLE_ASSIGNMENTS=
# Exact origins, comma separated. Compared as origins, never as prefixes.
ALLOWED_ORIGINS=
